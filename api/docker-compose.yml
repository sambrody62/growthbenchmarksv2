version: "3.8"

services:
  api:
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 512M
    build:
      context: .
      dockerfile: Dockerfile.dev
    env_file:
      - ".env"
    volumes:
      - "./:/fathom"
    ports:
      - "8000:8080"
    restart: always
